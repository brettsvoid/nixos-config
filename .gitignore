# Auto-generated by git-hooks.nix's installationScript on `nix develop`.
# Symlinks into /nix/store; per-machine; do not commit.
.pre-commit-config.yaml

# direnv local state
.direnv/
.DS_Store

# nix build outputs (`darwin-rebuild build`, `nix build`); symlinks into
# /nix/store, per-machine, never source
result
result-*

# Rust build artifacts (stray cargo / rust-analyzer targets)
target/
